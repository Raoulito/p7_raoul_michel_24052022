import { createTheme } from "@mui/material";

export const theme = createTheme({
    typography: {
        fontFamily: "lato",
    },

    palette: {
        primary: {
            main: "#ffd7d7",
        },
        secondary: {
            main: "#4e5166",
        },
        third: {
            main: "#ffffff"
        }
    },
});